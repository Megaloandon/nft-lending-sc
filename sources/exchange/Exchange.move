module lending_addr::exchange {
    use lending_addr::storage;

    //============================================================================
    //=============================== Entry Fucntion =============================
    //============================================================================
    
    public entry fun sell_offer_nft(sender: &signer, token_id: u64, floor_price: u256) {
        // deposit NFT to contract
        

        // store offer nft to storage

        // create queue offer 
    }
}